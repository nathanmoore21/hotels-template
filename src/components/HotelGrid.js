// import React from 'react';
// import { Link } from 'react-router-dom';

// const HotelGrid = ({ hotels }) => {
//   return (
//     <div>
//       {hotels.map((hotel, index) => (
//         <div key={index}>
//           <Link to={`/hotel/${encodeURIComponent(hotel["hotel_name"])}`}>
//             {hotel["hotel_name"]}
//           </Link>
//         </div>
//       ))}
//     </div>
//   );
// };

// export default HotelGrid;
